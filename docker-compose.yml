version: "3.9"

services:
  arb-bot:
    build: .
    container_name: arb-bot
    ports:
      - "8080:8080"
    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/arbitragem"]
      interval: 30s
      timeout: 5s
      retries: 3
